/* eslint-disable react/prop-types */
import React from 'react';
import Navigation from '../card/navigation/Navigation.jsx';

const Header = ({ searchData }) => (
  <Navigation searchData={searchData} />
);

export default Header;
